# apps/api — Übergangs-API (NestJS)

> ⚠️ **Nicht produktiv.** Diese NestJS-App ist ein Übergangspfad aus der frühen Entwicklungsphase und läuft nicht in Produktion.

## Produktive API

Alle Endpunkte sind in **`apps/web/src/app/api/v1/`** als Next.js Route Handler implementiert und produktiv.

## Neue Endpunkte

Ausschließlich in `apps/web/src/app/api/v1/` anlegen.
