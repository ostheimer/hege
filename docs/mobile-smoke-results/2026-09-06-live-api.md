# Live-API und iOS-Startseite

## Deployment

- URL: https://hege.app
- Ziel: Produktion, Status READY
- Server-Commit: a86ae2b
- Deployment: dpl_AX1npp6gMeyCMG2ssXJptFxqs1DY
- Framework: Next.js 15.5.14; Build rund eine Minute
- Vorheriges Deployment für Rollback: dpl_77yCARCWPHNV6UwpbyjJouUCJdXu

Die fehlenden Routen `/api/v1/activities` und `/api/v1/revier-map` verursachten HTTP 404 in der installierten App. Beide sind jetzt live. Authentifiziert wurden Dashboard, Aktivitäten, Revierkarte und Einrichtungen mit HTTP 200 und JSON geprüft. Ohne Anmeldung liefern die neuen Endpunkte HTTP 401.

Die additiven Migrationen 0012/0013 wurden erst gegen eine lokale Kopie des Produktionsschemas samt Migrationsjournal geprüft und dann über Drizzle direkt in Produktion angewendet. Keine Nutzerdaten exportiert oder neu eingesät; Benutzer-, Mitgliedschafts- und Revieranzahl unverändert. Die Revierkarte liefert noch `map: null`, da der lokale Kartenimport nicht Teil dieses Rollouts war.

## Mobile

Die optionale Aktivitäten-Historie darf das Dashboard bei Teilfehlern nicht mehr verwerfen. Authentifizierungs-/Berechtigungsfehler werden nicht verschluckt. HTTP-Fehler werden nicht pauschal als offline bezeichnet. Regressionstest zunächst rot, dann grün; 207 Mobile-Tests, Typecheck und 299 Web-Tests bestanden.

OTA veröffentlicht für iOS, Preview, Runtime 1.0.1:
https://expo.dev/accounts/aostheimer/projects/hege-revier/updates/b0b311e7-c380-48b7-80ee-8b642228ac92
Kein neuer EAS-Build. Empfang des OTA-Updates auf dem physischen iPhone noch nicht bestätigt.

Maestro-Live-Login zeigte die personalisierte Startseite mit Aktivitäten und ohne API-Fehler. Screenshot geöffnet und visuell geprüft. Danach trat ein Simulator-Crash in `XCTAutomationSession` auf (Issue #218). Der Neustart ohne Testtreiber blieb während der anschließenden Prüfung stabil. Dies ist keine vollständige Behebung des Automatisierungsfehlers.

## Verbleibende Grenzen

- PR #217: Preview-Smoke scheitert am Login mit HTTP 500; die Preview-Umgebung enthält keine DATABASE_URL. Keine Produktions-DB an automatisierte schreibende Preview-Smokes angeschlossen; kein erzwungener Merge.
- Issue #218: XCTest-/Simulator-Crash weiter isolieren.
- Eine Diagnoseausgabe enthielt versehentlich einen Datenbank-Zugangswert; dieser wurde nicht in Code oder Dokumentation übernommen. Keine eigenmächtige Rotation der Zugangsdaten.
- Produktionsprüfung umfasst konkrete API-Abrufe, keinen vollständigen historischen Log-Scan. Drains/weiteres Monitoring wurden nicht verändert.
